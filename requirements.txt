# 工业数据分析技能 - 依赖清单
# 安装：pip install -r requirements.txt

# === MCP 框架（必须）===
mcp>=1.27.0
anyio>=4.13.0
sniffio>=1.3.1
pydantic-settings>=2.14.0

# === 数据科学（必须）===
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
statsmodels>=0.14.0
joblib>=1.3.0

# === 可视化（必须）===
matplotlib>=3.7.0
seaborn>=0.13.0

# === 报告生成（必须）===
python-docx>=1.0.0
openpyxl>=3.1.0
lxml>=4.9.0

# === 数据库连接（可选，用于 SQL 数据提取功能）===
# 安装前确保 PostgreSQL 已安装，安装后可能需要重启终端
pymysql>=1.1.0          # MySQL
psycopg2-binary>=2.9.0  # PostgreSQL
# influxdb-client       # InfluxDB (时序数据库)
# pymongo

# === 可选：深度学习（钢板缺陷检测）===
# torch
# torchvision
